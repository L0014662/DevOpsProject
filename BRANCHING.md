# Branching Strategy

For this project, I've decided to use **Trunk-Based Development (TBD)** that makes use of short-lived branches for features that is later merged back into the `main` branch.
This strategy allows for smaller and frequent changes as opposed to larger risker merges, less likely to cause merge conflicts and is suitable for the Continuous Integration workflow.